import SwiftUI
import shared

@main
struct iOSApp: App {
    init() {
        // Any global initialization for KMP shared module if needed
    }

    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}
